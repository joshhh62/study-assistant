# Only needed for production deploys where frontend and backend are two
# separate hosted services (e.g. Vercel + Render). Leave unset for local
# dev — Vite's dev server proxy handles it instead (see vite.config.js).
VITE_API_BASE_URL=
